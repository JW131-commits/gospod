<!DOCTYPE html>
<html lang="en">
<head>
    <%-include('partials/head.ejs')%>
    <title>Document</title>
</head>
<body>
    <%-include('partials/header.ejs')%>
    <div class="container">
        <div class="main">
            
            <form action="/api/signin" method="POST" class="form">
                <h3>Войти</h3>
                <fieldset class="fieldset">
                    <label for="">E-mail</label>
                    <input type="email" placeholder="Введите почту" name="email">
                    <%
                        if(message){
                    %>
                            <div class="errors">
                                <p> <%=message%></p>
                            </div>
                    <%
                        }
                    %>
                </fieldset>
                
               
                <fieldset class="fieldset">
                    <label for="">Пароль</label>
                    <input type="password" placeholder="Введите пароль" name="password">
                    <% 
                        if(error){
                    %>
                        <div class="errors">
                            <p> <%=error%></p>
                        </div>
                    <%
                            }
                    %>
                </fieldset>
               
                <fieldset class="fieldset">
                    <button type="submit">Войти</button>

                </fieldset>
                
                <p>У вас нет аккаунта <a href="/register">Зарегистрироваться</a></p>
               
                <div class="google">
                    <p>Войти с помошью: </p>
                    <a href="/api/auth/google">  <img src="/images/icons/google.png" alt=""></a>
                  
                </div>
               
            </form>
        </div>
    </div>
    <%- include('partials/scripts.ejs')%>
</body>
</html>